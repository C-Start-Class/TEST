#include <stdio.h>

int main()
{
    int n;
    int sum = 0;
    printf("Nhap gia tri n: ");
    scanf("%d",&n);
    for(int i = 2; i < n; i++){
        if(n%i==0&&i%2==0){
             sum+=i;
        }
    }
        printf("Tong cac uoc so chan la %d",sum);
}
