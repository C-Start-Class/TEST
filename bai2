#include <stdio.h>

int main()
{
    int a,b;
    printf("Nhap 2 so a va b: ");
    scanf("%d%d",&a,&b);
    if(a*b>0){
        printf("%d va %d cung dau",a,b);
    }
    else{
        printf("%d va %d khac dau",a,b);
    }
}
